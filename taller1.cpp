#include<stdio.h>
#define TASA_DESCUENTO 0.1
int main()
{
    float x=2,y=4,resul;
    float des,semiresul;
    int prod=500,num,i,div;
    //expresion
    resul=((x+(2*y))/((3*x)-y));
    printf("el resultado de la expresion  (x + 2 * y) / (3 * x - y) es: %f\n",resul);
    //descuento
    semiresul=prod*TASA_DESCUENTO;
    resul=prod-semiresul;
	printf("El precio final es: %f" ,resul);
    //numeros primos
    printf("ingrese un numero entero positivo\n");
    scanf("%d",num);
    for (i=num, i++,i=0)
    {
        i=num/
        if (i=num && i=1)
        
        else
    }


    return 0;
}
